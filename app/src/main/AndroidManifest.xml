<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROCESSING" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="Kepler Studio"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Exynos ENN vendor public library: link-time visibility for libenn_public_api_ndk_v1.so.
              On devices exposing this library in /vendor/etc/public.libraries.txt the stub
              can be loaded by any third-party APK that declares this dependency.
              Marked optional so KeplerStudio remains installable on non-Exynos devices. -->
        <uses-native-library
            android:name="libenn_user.samsung_slsi.so"
            android:required="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".editor.SuperResolutionMediaProcessingService"
            android:exported="false"
            android:foregroundServiceType="dataSync|mediaProcessing" />

    </application>
</manifest>
